<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- NSAudioCaptureUsageDescription must live in the file directly, not as
	     an INFOPLIST_KEY_* build setting — Xcode 26's auto-translation table
	     does not recognise this key, so a build-setting value silently fails
	     to land in the embedded plist. Without this key present, TCC
	     auto-denies `TCCAccessRequest("kTCCServiceAudioCapture")` in ~7 ms
	     without showing the user any prompt, which breaks Core Audio
	     Process Taps entirely. -->
	<key>NSAudioCaptureUsageDescription</key>
	<string>High Videlity captures audio from whatever app you're playing music in (Apple Music, Spotify, browser, etc.) so the visualizer can react to it frame-accurately, with no third-party loopback driver required.</string>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationPreferredDefaultSceneSessionRole</key>
		<string>UIWindowSceneSessionRoleApplication</string>
		<key>UIApplicationSupportsMultipleScenes</key>
		<true/>
		<key>UISceneConfigurations</key>
		<dict>
			<key>UISceneSessionRoleImmersiveSpaceApplication</key>
			<array>
				<dict>
					<key>UISceneInitialImmersionStyle</key>
					<string>UIImmersionStyleMixed</string>
				</dict>
			</array>
		</dict>
	</dict>
</dict>
</plist>
